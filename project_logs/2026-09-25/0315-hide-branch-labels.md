วันที่และเวลา: 2026-09-25 03:15

สรุปสิ่งที่แก้ไข:
- ลบการแสดงชื่อ branch เช่น `1A` และ `1A_2A` ออกจากการ์ดแผนที่ใน `client/src/pages/MiNi_Game.jsx`
- ใช้ label กลางเป็น `ด่าน N` หรือ `จุดจบ` แทน
- คงข้อมูล branch ภายในและเส้นเชื่อมของกราฟไว้เหมือนเดิม

เหตุผลของการเปลี่ยนแปลง:
- ไม่ต้องการเปิดเผย key ภายในของเส้นทางบทสนทนาใน UI

ผลกระทบที่อาจมีต่อส่วนอื่นของระบบ:
- เปลี่ยนเฉพาะข้อความที่มองเห็นบนการ์ดแผนที่
- ไม่เปลี่ยนการคำนวณหรือการเลือกเส้นทางบทสนทนา

การตรวจสอบ:
- VS Code diagnostics: ไม่พบ errors ใน `MiNi_Game.jsx`
