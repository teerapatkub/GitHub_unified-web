วันที่และเวลา: 2026-09-23 22:22

สรุปสิ่งที่แก้ไข/เพิ่มเข้ามา:
- แก้ `client/public/WebTutorialModal/webTutorials.js` ให้ step `learning-progress` ใช้ field `image`
- ตั้งค่า image เป็น `/WebTutorialModal/data_png/learning-progress.png`
- ลบการใช้ field `Optional` และค่า `image: null`

เหตุผลของการเปลี่ยนแปลง:
- `WebTutorialModal.jsx` อ่านเฉพาะ `step.image` จึงไม่แสดงรูปเมื่อข้อมูลใช้ชื่อ `Optional` และกำหนด image เป็น null

ผลกระทบที่อาจมีต่อส่วนอื่นของระบบ:
- รูปตัวอย่างความคืบหน้า XP จะแสดงในขั้นตอนแรกของบทสอนหน้า Learning
- ไม่มีการเพิ่มตารางหรือ API

สิ่งที่ยังไม่เสร็จหรือต้องทดสอบเพิ่มเติม:
- หากเพิ่มรูปให้ขั้นตอนอื่น ต้องใช้ field `image` เช่นเดียวกัน
- ควร hard refresh browser เพื่อป้องกัน cache ของไฟล์ public เดิม

ผลการตรวจสอบ:
- `npm run build` ผ่าน
- ตรวจพบไฟล์ `dist/WebTutorialModal/data_png/learning-progress.png` แล้ว
