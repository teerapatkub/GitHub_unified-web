วันที่และเวลา: 2026-09-18

สรุปการแก้ไข:
- แก้ไข `client/src/pages/MiNi_Game.jsx`
- เมื่อกด DONE ให้เรียก `runDoneChecks()` โดยตรง
- ยกเลิกการเรียก `runCodeInStory()` ในขั้นตอนส่งคำตอบ เพื่อไม่ถาม input จากผู้ใช้ซ้ำ
- ระบบจะป้อนค่าจาก `correctness` อัตโนมัติและเปรียบเทียบกับ `expected` ทุกกรณี

เหตุผล:
- ผู้ใช้ต้องการให้ระบบรันโค้ดเองด้วย input ที่กำหนดใน test cases แล้วตรวจคำตอบอัตโนมัติ

ผลกระทบ:
- ปุ่ม DONE ตรวจ input 100, 250, 500, 700 และ 1000 ตาม JSON ที่กำหนด
- กฎ branch ยังใช้ผลลัพธ์จากชุดทดสอบอัตโนมัติ

หมายเหตุ:
- test case input 1000 กำหนด expected เป็น `ราคารวมทั้งหมดคือ: 107.0` แต่การคำนวณ `1000 * 1.07` ได้ `1070.0` จึงจะถูกตัดสินว่าไม่ผ่านตามข้อมูลที่กำหนดไว้

การตรวจสอบ:
- `npm run build` ผ่านสำเร็จ
