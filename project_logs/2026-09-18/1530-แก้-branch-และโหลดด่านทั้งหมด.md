วันที่และเวลา: 2026-09-18

สรุปการแก้ไข:
- แก้ไข `server/server.js` เอา `LIMIT 3` ออกจาก query ด่าน Mini Game เพื่อให้ API ส่งด่านปลายทางทั้งหมด
- แก้ไข `client/src/pages/MiNi_Game.jsx` ให้เก็บ input ที่ผู้ใช้กรอกใน Story Mode
- กฎที่ใช้ `float` จะประเมินจาก input จริง เช่น 500 ไม่ใช่ตัวเลขจาก output เช่น 535.0
- กฎแบบข้อความยังประเมินจาก output ของโปรแกรมตามเดิม

ผลลัพธ์:
- เส้นทาง `START -> 1A/1B -> 1A_2A/1A_2B/1B_2A/1B_2B` มีข้อมูลครบใน client
- การกรอกค่าต่ำกว่าหรือเท่ากับ 500 จะไป branch `1B`
- การกรอกค่ามากกว่า 500 จะไป branch `1A`

การตรวจสอบ:
- `node --check server.js` ผ่าน
- `npm run build` ผ่าน
