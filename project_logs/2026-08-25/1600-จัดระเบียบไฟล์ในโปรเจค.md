# จัดระเบียบไฟล์ในโปรเจค

**วันที่:** 2026-08-25 เวลา 16:00
**ผู้ทำ:** Person 3
**เหตุผล:** ผู้ใช้แจ้งว่าไฟล์เยอะและไม่เป็นระเบียบ ให้ย้ายของที่ไม่ใช้แล้วไปโฟลเดอร์เฉพาะหรือลบออก

---

## 1. รากโปรเจคก่อน/หลัง

**ก่อน** — 15 รายการปนกัน รวมไฟล์ zip 2 ก้อน 298 MB
**หลัง** — เหลือ 9 รายการ: `client/` `server/` `shared/` `docs/` `specs/` `project_logs/`
`CLAUDE.md` `CONTEXT.md` `AGENTS.md` (+ `_archive/` ที่ gitignore ไว้)

## 2. สิ่งที่ทำ

### ของหนักที่ไม่อยู่ใน git → `_archive/` (ราว 450 MB)

`GitHub_unified-web-Project2-tee.zip` (151 MB) · `GitHub_unified-web-main.zip` (146 MB) ·
`GitHub_unified-web-Project2-tee/` · `nothing/` · `20.8.2569.sql` · สำเนา `.env` เก่า

**ย้าย ไม่ได้ลบ** เพราะของพวกนี้ไม่มีสำเนาอยู่ที่อื่นเลย มี `_archive/README.md`
บอกว่าแต่ละอันคืออะไรและอันไหนลบได้แล้วบ้าง

### โค้ดที่ตายแล้วในฝั่ง client

ลบ `JoinRoom.jsx` `Lobby.jsx` `Matchmaking.jsx` `OnlineMenu.jsx` — ยืนยันแล้วว่า
ไม่มีไฟล์ใด import ทั้งสี่ตัว (ประวัติยังอยู่ใน git)

ย้าย `ShopPage.jsx` และ `Achievements.jsx` ซึ่ง **ยังถูกใช้งานจริงจาก `App.jsx`**
ขึ้นมาไว้ที่ `client/src/pages/` แล้วลบโฟลเดอร์ `old plan` ทิ้งทั้งสองที่
(ทั้ง `pages/` และ `components/`) — ชื่อโฟลเดอร์ทำให้เข้าใจผิดว่าไฟล์ข้างในไม่ได้ใช้แล้ว

### สคริปต์ที่ใช้ครั้งเดียว → `server/scripts/archive/`

16 ไฟล์ที่ไม่มี `package.json`, `server.js` หรือ `db.js` อ้างถึงเลย ส่วนใหญ่เป็นสคริปต์
seed เนื้อเรื่องมินิเกมและสคริปต์ย้ายฐานข้อมูลจาก MySQL **เก็บไว้ไม่ลบ** เพราะเป็นบันทึกว่า
ข้อมูลในฐานวันนี้มาจากไหน — `db.js` ไม่ได้ seed เนื้อเรื่องมินิเกมให้เองตอนบูต
มี `README.md` อธิบายไว้ในโฟลเดอร์

หลังย้าย `server/scripts/` เหลือแต่ไฟล์ที่ถูกเรียกจาก npm scripts จริงทั้งหมด

### เอกสาร

`plan.md` (48 KB) และ `TODO.md` (19 KB) → `docs/archive/` (ถูกแทนที่ด้วย spec แล้ว
แต่เก็บไว้เพราะมีบันทึกการทดสอบของเฟสเก่าและ Verification policy ที่ยังใช้อยู่)
`test.md` → `docs/arcade-solutions.md` (ชื่อเดิมไม่บอกว่าเป็นอะไร)

## 3. ทดสอบ

| ชุด | ผล |
|---|---|
| `npm run build` | **ผ่าน** (เจอ import พังหนึ่งจุดตอนย้ายไฟล์ แก้แล้ว — ดูข้อ 4) |
| lint | 60 error / 8 warning (ลดจาก 61/9 เพราะลบไฟล์ตายทิ้ง) |
| `test:problems` · `test:grader` · `test:submit` | 15/15 · 9/9 · 7/7 |
| `test:arcade` | 7/7 · 7/7 |
| `test:shop` · `test:achievements` | 11/11 · 8/8 (สองชุดนี้ครอบไฟล์ที่ย้าย) |
| `check:sql` · `check:undef` | 0 rejected · ผ่าน |

## 4. บั๊กที่เกิดจากการย้ายเอง และแก้แล้ว

`ShopPage.jsx` มี `import ... from '../../contexts/ThemeContext'` พอย้ายขึ้นมาหนึ่งชั้น
path เดิมชี้ออกนอกโฟลเดอร์ `src/` → build พัง แก้เป็น `../contexts/ThemeContext`
**จับได้เพราะรัน `npm run build` หลังย้าย** ไม่ใช่เพราะอ่านโค้ด — lint ไม่จับ

## 5. สิ่งที่ยังไม่ทำ

- ยังไม่ลบอะไรใน `_archive/` เลย ต้องให้ผู้ใช้ยืนยันก่อน โดยเฉพาะ `20.8.2569.sql`
  ที่มีข้อมูลผู้ใช้จริง
- `CLAUDE.md` เคยอ้างถึง `prototype/` ซึ่งไม่มีอยู่จริงแล้ว แก้เป็นรายการโฟลเดอร์ปัจจุบันแทน
